# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (13):**
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- qr-no-duplicate-imports (`src/main.rs`): Duplicate import: use opentelemetry::trace::Tracer as _;
- qr-no-duplicate-imports (`src/main.rs`): Duplicate import: use opentelemetry::trace::Span as _;
- qr-no-duplicate-imports (`src/main.rs`): Duplicate import: use opentelemetry::trace::Span as _;
- qr-not-a-noop (`Cargo.toml`): The final Cargo.toml enables opentelemetry-otlp features "reqwest-client" (async) plus "reqwest-blocking-client" but the exporters are built with `.with_http()` and the SdkMeterProvider uses `.with_periodic_exporter`/SdkTracerProvider uses `.with_batch_exporter`, which require an async runtime-compatible HTTP client; a blocking reqwest client invoked from within the tokio runtime can panic ("cannot block the current thread from within a runtime") and drop telemetry.
- qr-preserves-behavior (`src/main.rs`): In the middleware layer added after `.with_state(db)` (final version places the middleware `.layer(...)` before `.with_state(db)`), the active_requests up/down counter is incremented before `next.run(req)` and decremented after; if `next.run` panics the decrement never runs, causing a permanent leak in the in-flight gauge. This gauge behavior is introduced by the new instrumentation.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- qr-no-duplicate-imports (`src/main.rs`): Duplicate import: use opentelemetry::trace::Span;
- qr-duplicate-metric (`src/main.rs`): Metric name "flow.outcomes" is created 2 times in this change (src/main.rs:99, src/main.rs:99). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-duplicate-metric (`src/main.rs`): Metric name "flow.duration" is created 2 times in this change (src/main.rs:105, src/main.rs:105). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-duplicate-metric (`src/main.rs`): Metric name "flow.validation.outcomes" is created 2 times in this change (src/main.rs:111, src/main.rs:111). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-not-a-noop (`Cargo.toml`): The final Cargo.toml enables the `reqwest-client` feature (async reqwest) but the code never builds a tokio runtime for the exporter beyond the tokio::main; with `with_periodic_exporter`/`with_batch_exporter` the async reqwest client requires a running runtime, whereas the recipe/proposedDependencies specified `reqwest-blocking-client`. Feature drift here can cause the exporter to silently fail to export outside a compatible runtime context.
- qr-not-a-noop (`src/main.rs`): The Telemetry struct holds flow_outcomes/flow_duration/validation_outcomes instruments built in init_telemetry(), but request handlers use a separate FlowTelemetry built again in main(); the Telemetry copies of these three instruments are never used, so those fields are dead and no telemetry is emitted through them.
**Outstanding (6):**
- ⚠ qr-duplicate-metric (`src/main.rs`:92): Metric name "flow.outcomes" is created 3 times in this change (src/main.rs:92, src/main.rs:92, src/main.rs:92). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ. — _Create the "flow.outcomes" instrument ONCE (a single shared meter/instrument reused across the call sites) and record to it from each place, rather than registering it in multiple files._
- ⚠ qr-duplicate-metric (`src/main.rs`:98): Metric name "flow.duration" is created 3 times in this change (src/main.rs:98, src/main.rs:98, src/main.rs:98). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ. — _Create the "flow.duration" instrument ONCE (a single shared meter/instrument reused across the call sites) and record to it from each place, rather than registering it in multiple files._
- ⚠ qr-duplicate-metric (`src/main.rs`:104): Metric name "flow.validation.outcomes" is created 3 times in this change (src/main.rs:104, src/main.rs:104, src/main.rs:104). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ. — _Create the "flow.validation.outcomes" instrument ONCE (a single shared meter/instrument reused across the call sites) and record to it from each place, rather than registering it in multiple files._
- ⚠ qr-not-a-noop (`src/main.rs`:72): The metric exporter is built with `.with_http()` using the `reqwest-blocking-client` feature, and the SdkMeterProvider uses a periodic exporter whose background task runs inside the Tokio runtime. A blocking reqwest client invoked from an async runtime thread can panic/stall metric export, potentially preventing metrics from being emitted. — _Use a non-blocking reqwest client feature (e.g. `reqwest-client`) compatible with the async periodic/batch exporters, or export metrics from a dedicated thread._
- ⛔ qr-preserves-behavior (`src/main.rs`:170): The added `axum::middleware::from_fn` layer is attached AFTER `.with_state(db)` and OUTSIDE the `ServiceBuilder` that installs the `HandleErrorLayer`/`timeout`. This changes the middleware ordering versus the original composition and, because the from_fn closure returns the raw `Response`, the newly-added layer wraps the whole stack in a way that alters request handling ordering relative to the pre-existing timeout/trace layers. — _Place the metrics middleware inside the existing ServiceBuilder chain (or before .with_state) so ordering matches intent, and confirm the timeout/error handling still wraps the instrumented path as before._
- ⚠ qr-preserves-behavior (`src/main.rs`:250): The validation spans in todos_create and todos_update are started (`_validation_span`) but never explicitly ended; they are only dropped at function scope end, so the span covers the entire handler rather than just validation, and it is not entered as the active context, so child spans/events are not associated. — _Scope the span to the validation step (drop it after validation) and mark it active via a context guard so it accurately measures validation._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = { version = "0.28.0", features = ["trace"] }`
- `opentelemetry_sdk = { version = "0.28.0", features = ["trace", "metrics"] }`
- `opentelemetry-otlp = { version = "0.28.0", default-features = false, features = ["http-proto", "reqwest-blocking-client"] }`
