# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (10):**
- qr-not-a-noop (`src/main.rs`): This is an app-sdk binary but main() never registers any OTel SDK — no SdkMeterProvider is built or set via global::set_meter_provider. The counter emitted in todos_update resolves to a no-op global meter and no metrics are ever exported.
- qr-preserves-behavior (`src/main.rs`): The counter's semantic meaning is wrong: it fires on every update where the todo ends up completed (including updates of already-completed todos or edits that were never part of a create-and-complete flow), so it does not measure terminal outcomes of that flow and produces misleading telemetry.
- qr-not-a-noop (`src/main.rs`): The instrumentation uses global::meter and KeyValue but no corresponding `use opentelemetry::{global, KeyValue};` import is added, so the symbols are undeclared.
- general (`Cargo.toml`): The declared opentelemetry-otlp features (http-proto + reqwest-blocking-client) provide a blocking HTTP client, but the wiring recipe's PeriodicReader for metric export requires an async runtime feature (rt-tokio); with a blocking client and no rt-tokio the periodic metrics exporter will not run correctly under the tokio runtime.
- edit-not-applied (`src/main.rs`): An intended edit to src/main.rs could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: Consolidate the six conflicting src/main.rs variants into a single coherent file: register the OTel SDK (meter+tracer providers) once in main() with shutdown on exit, wire the HTTP metrics middleware into the router, fix imports (global, KeyValue, atomics, Instant, MatchedPath, middleware, Resource, trace traits), and keep the flow-level counters in todos_create/todos_update using the now-imported global/KeyValue.
- qr-duplicate-file-path (`src/main.rs`): "src/main.rs" appears 6× in the changeset (edited file + edited file + edited file + edited file + edited file + edited file) — two conflicting full contents for one path. Only the last-written wins, and it may drop declarations/imports the other version added, so the file won't compile.
- qr-not-a-noop (`src/main.rs`): The final version of src/main.rs (last diff entry) still uses global::meter("axum-todo") in todos_update to emit flow.outcomes, but main() never builds or registers any SdkMeterProvider/SdkTracerProvider. Under the app-sdk model this is the diff that must register the SDK; without it every counter resolves to a no-op and emits nothing.
- qr-not-a-noop (`src/main.rs`): The final version references global, KeyValue and Uuid-based counter usage in todos_update but the file's use block omits `use opentelemetry::{global, KeyValue};`, so global and KeyValue are undeclared.
- general (`src/main.rs`): The registration variant of main.rs uses AtomicI64, Ordering, MatchedPath, Request, Next, Instant, Tracer/Span/Status and opentelemetry_sdk::Resource without importing them; ACTIVE_REQUESTS/WORKER_POOL_SIZE and the middleware are referenced but not consistently declared across the presented file variants.
- general (`Cargo.toml`): opentelemetry-otlp is configured for http-proto with reqwest-blocking-client but PeriodicReader's periodic metric export requires the async runtime feature (rt-tokio); a blocking reqwest client with no rt-tokio feature will not drive periodic metric export under Tokio.
**Outstanding (4):**
- ⛔ qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration. — _Make build-file edits additive: oldString = a small existing anchor, newString = that anchor plus the new dependency line(s). Never drop or rewrite an existing line._
- ⚠ general (`Cargo.toml`:18): opentelemetry-otlp enables both `reqwest-client` (async) and `reqwest-blocking-client` features while the SdkMeterProvider uses a PeriodicReader that requires the rt-tokio async runtime feature; a blocking reqwest client combined with a missing rt-tokio feature can produce a non-functional or panicking exporter at runtime. — _Pick one reqwest transport (the async `reqwest-client`) and add the `rt-tokio` runtime feature required by the periodic metric reader._
- ⚠ qr-not-a-noop (`Cargo.toml`:18): The metric PeriodicReader (with_periodic_exporter) requires an async runtime feature (e.g. rt-tokio) which is not enabled in the opentelemetry-otlp/opentelemetry_sdk feature set, so periodic metric export may never fire and metrics silently fail to emit. — _Enable the rt-tokio feature so the periodic reader can schedule exports on the Tokio runtime._
- ⚠ qr-preserves-behavior (`src/main.rs`:42): The OTLP metric and span exporters are built with `.expect(...)`, so if the exporter cannot be constructed (e.g. bad endpoint/config) main() panics on startup, changing the application from starting normally to crashing. — _Handle the exporter build error gracefully (log and continue without telemetry, or propagate via Result) instead of panicking in main()._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-client", "metrics", "trace"] }`
