# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- general (`src/main.rs`): The router chains `.with_state(db)` followed by `.with_state(metrics)`, but `todos_index`/`todos_create`/etc. only extract `State<Db>`. Two `with_state` calls of different types on the same Router is a type/state-mismatch that will not compile — `AppMetrics` is never used as extracted state and the second `with_state` conflicts with the fully-specified Db state.
- qr-preserves-behavior (`src/main.rs`): In `todos_create` the success path returns `(StatusCode::CREATED, Json(todo))` while the validation-failure path returns `(StatusCode::UNPROCESSABLE_ENTITY, Json(Todo{...}))`; both arms must produce the same tuple type, but they do — however the original endpoint's `-> impl IntoResponse` now returns a placeholder `Todo` with empty fields on 422 instead of a proper error body, changing the response contract and returning fabricated data.
- qr-preserves-behavior (`src/main.rs`): The `meter_provider.shutdown()`/`tracer_provider.shutdown()` calls are placed after `axum::Server::...await.unwrap()`, which for a long-running server never returns during normal operation, so metrics/spans are never flushed on shutdown.
- qr-preserves-behavior (`src/main.rs`): The `AppMetrics` struct and its instruments are constructed and passed as state but never used by any handler (handlers rebuild their own instruments each request), so the created fields are dead code that will trigger unused-field warnings and defeat the intended shared-instrument design.
- general (`src/main.rs`): The metrics SDK uses features ["metrics","trace","rt-tokio"] and .with_periodic_exporter, but SdkMeterProvider's periodic reader requires an async runtime. With opentelemetry-otlp configured for reqwest-blocking-client and no rt-tokio runtime bridge wired into the periodic reader, the periodic metric export needs a runtime; more critically the delete handler's status_code type mismatch below must also be checked.
- qr-preserves-behavior (`src/main.rs`): In todos_update and todos_get, the metric-recording closure is passed to Result::ok_or_else, so the active_requests decrement and request metrics only execute when the todo is NOT found; on the success path the KeyValue array construction is fine but the closure is never invoked, which is intended, however the borrow of `metrics` inside the FnOnce closure while `metrics` is also used later after `?` can cause a borrow/move conflict. Confirm compilation: `metrics` is used by reference in the closure and again after, which is valid, but note the closure captures metrics by reference and returns StatusCode — this is acceptable.
- general (`Cargo.toml`): The proposed dependency list includes serde_json but Cargo.toml never adds serde_json to [dependencies], while src/main.rs uses serde_json::json! in todos_create; this will fail to compile with an unresolved crate `serde_json`.
**Outstanding (2):**
- ⛔ qr-preserves-behavior (`src/main.rs`:1): The `#[tokio::main]` runtime plus `with_periodic_exporter`/`with_batch_exporter` require the SDK's tokio runtime, but the metric and span exporters are configured with the `reqwest-blocking-client` feature (a blocking HTTP client). Using a blocking reqwest client inside the async tokio runtime will panic at runtime ("Cannot drop a runtime in a context where blocking is not allowed" / blocking call in async context), preventing telemetry export. — _Use the non-blocking `reqwest-client` feature (with `rt-tokio` runtime) for the OTLP exporter instead of `reqwest-blocking-client`, so exports run on the async runtime._
- ⛔ qr-preserves-behavior (`src/main.rs`:1): In todos_update and todos_get, the `active_requests.add(-1, ...)` decrement is only performed inside the `ok_or_else` 404 closure and on the success path; however the `?` operator returns early on the 404, and the success path also decrements — this is fine, but for todos_update the decrement in the success path executes normally. This is not the issue.

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = { version = "0.28.0", features = ["metrics", "trace"] }`
- `opentelemetry_sdk = { version = "0.28.0", features = ["metrics", "trace", "rt-tokio"] }`
- `opentelemetry-otlp = { version = "0.28.0", features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
- `serde_json`
