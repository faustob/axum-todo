# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = "0.31"", "opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration.
- qr-not-a-noop (`src/main.rs`): The periodic metric reader (with_periodic_exporter) requires the async-runtime feature rt-tokio on opentelemetry_sdk to spawn its background export task, but Cargo.toml's final content pins opentelemetry_sdk = { version = "0.31", features = [] } with no rt-tokio feature. Without the runtime feature the PeriodicReader cannot drive exports, so metrics are never flushed to the collector.
- general (`Cargo.toml`): opentelemetry-otlp is configured with both "reqwest-client" (async) and "reqwest-blocking-client" features simultaneously; combined with http-proto this mixes blocking and async HTTP clients for OTLP export and can pull in a blocking client that stalls the async runtime or produce conflicting client selection.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = "0.31"", "opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration.
- qr-not-a-noop (`src/main.rs`): The PeriodicReader created by SdkMeterProvider::with_periodic_exporter requires the async runtime feature (rt-tokio) on opentelemetry_sdk, but the final Cargo.toml declares opentelemetry_sdk = { version = "0.31", features = [] } with no rt-tokio. Without the runtime feature the periodic metric reader cannot run its export loop, so metrics never flush and the instrumentation emits nothing.
- qr-not-a-noop (`Cargo.toml`): The opentelemetry-otlp features mix conflicting HTTP client backends by enabling both "reqwest-client" (async) and "reqwest-blocking-client"; combined with .with_http() the transport client selection is ambiguous and can fail to build or select an unintended client.
**Outstanding (3):**
- ⚠ general (`Cargo.toml`): The diff uses `once_cell::sync::Lazy` in src/main.rs and lists once_cell in proposedDependencies, but the Cargo.toml [dependencies] section does not add `once_cell = "1"`, so the crate will fail to resolve/compile. — _Add `once_cell = "1"` to Cargo.toml [dependencies]._
- ⚠ general (`Cargo.toml`): The PeriodicReader created via with_periodic_exporter requires the async runtime feature (rt-tokio) on opentelemetry_sdk, but opentelemetry-otlp uses the blocking reqwest client and no rt-tokio feature is enabled anywhere, which can prevent the periodic metric reader from exporting. — _Enable the `rt-tokio` feature on opentelemetry_sdk (and ensure the exporter transport features are consistent with the reader runtime)._
- ⛔ qr-not-a-noop (`src/main.rs`): The MetricExporter/SpanExporter builders use `.with_http()` but the opentelemetry-otlp dependency enables only `http-proto` + `reqwest-blocking-client`; the blocking client combined with the tokio-driven PeriodicReader/BatchProcessor means metric/span export runs a blocking HTTP call inside the async runtime, which can stall or fail to flush telemetry. — _Use a non-blocking reqwest client feature (e.g. `reqwest-client`) with the rt-tokio runtime, or otherwise ensure the exporter transport matches the async batch/periodic reader runtime._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
- `once_cell = "1"`
