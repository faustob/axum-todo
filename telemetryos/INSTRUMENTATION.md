# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (8):**
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = { version = "0.31", ", "opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration.
- qr-preserves-behavior (`src/main.rs`): otel_http_middleware enters a tracing span guard via `http_span.enter()` and holds the returned `_http_span_guard` across the `.await` on `next.run(req)`. Holding a non-Send span EnteredGuard across an await point in an async fn will fail to compile under tokio's multithreaded executor (future no longer Send), and if forced to compile leaks/keeps the span active across suspensions incorrectly.
- general (`Cargo.toml`): The final Cargo.toml enables both `reqwest-client` and `reqwest-blocking-client` features on opentelemetry-otlp; the http exporter is built with `.with_http()` (default reqwest client). The stated target requires PeriodicReader to use rt-tokio, but the final content lacks the `rt-tokio` feature on opentelemetry_sdk (one hunk adds it, but the file `content` shows opentelemetry_sdk without rt-tokio), so the periodic metric reader has no async runtime and metrics will not be exported.
- general (`src/main.rs`): The info_span macro declares a field named `error.type`, but tracing field names with dots must be quoted (`"error.type"`) or use the r# form; a bare dotted identifier as a field key is not valid macro syntax.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = { version = "0.31", ", "opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration.
- qr-not-a-noop (`Cargo.toml`): The PeriodicReader used by with_periodic_exporter requires an async runtime feature (rt-tokio) on opentelemetry_sdk, but the final Cargo.toml enables only ["metrics","trace"] without "rt-tokio". Without it the periodic metric reader has no runtime to drive exports and metrics will not be flushed on schedule.
- general (`Cargo.toml`): opentelemetry-otlp is configured with both "reqwest-client" (async) and "reqwest-blocking-client" in the final content across conflicting hunks; the second hunk switches to reqwest-client while the shown content lists reqwest-blocking-client, and a blocking reqwest client cannot drive the tokio-based periodic/batch exporters.
- qr-not-a-noop (`src/main.rs`): The tracing spans/events (info_span!, span.record, tracing::warn!) are emitted only through tracing-subscriber's fmt layer; no tracing-opentelemetry bridge layer is installed, so the registered SdkTracerProvider receives no spans from this middleware and produces no OTel trace telemetry.
**Outstanding (4):**
- ⚠ qr-not-a-noop (`Cargo.toml`:22): The opentelemetry-otlp feature `reqwest-blocking-client` provides a blocking HTTP client, but the SdkMeterProvider's PeriodicReader requires an async runtime (feature `rt-tokio`) and the SpanExporter's BatchProcessor likewise needs an async runtime. Without the tokio runtime feature enabled, the periodic metric reader/batch span processor cannot drive exports and telemetry will silently fail to be emitted. — _Use a tokio-compatible OTLP client (e.g. `reqwest-client`) and enable the `rt-tokio` feature on opentelemetry_sdk so the PeriodicReader/BatchProcessor have an async runtime to export on._
- ⛔ qr-not-a-noop (`src/main.rs`:118): MatchedPath is inserted into request extensions by axum's routing layer, but `otel_http_middleware` is attached via `.layer(middleware::from_fn(...))` applied to the whole Router OUTSIDE the route matching, so req.extensions().get::<MatchedPath>() is always None and every request records route="unmatched", making the http.route label useless and defeating the low-cardinality intent. — _Attach the middleware with `.route_layer(...)` or use middleware::from_fn positioned so it runs after route matching, so MatchedPath is present in the request extensions._
- ⚠ qr-not-a-noop (`src/main.rs`:133): The middleware calls tracing::Span::current().record("slow_request", true) / record("error.type", ...) on fields that were never declared on any span via a #[instrument] macro or span! field list; recording undeclared fields is a no-op in tracing, so these span attributes are never emitted. — _Create an explicit span with the fields declared (e.g. tracing::info_span!("http_request", slow_request = tracing::field::Empty, error.type = tracing::field::Empty)) and enter it before recording, or emit the data as event fields instead._
- ⚠ qr-not-a-noop (`src/main.rs`:195): The static instruments AUTH_ATTEMPTS_TOTAL and FLOW_ENTRIES_TOTAL/WORKER_POOL_SIZE_GAUGE are declared but AUTH_ATTEMPTS_TOTAL is never recorded anywhere, so the auth.attempts counter emits no data and is dead instrumentation. — _Either record auth.attempts at the authentication decision points or remove the unused instrument._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
- `once_cell = "1"`
