# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (9):**
- qr-preserves-behavior (`src/main.rs`): The router calls both `.with_state(db)` and `.with_state(telemetry)` which replaces the state type rather than combining them; axum's `with_state` sets a single state type, so this either fails to compile or overwrites `Db` state, breaking handlers that extract `State<Db>`.
- qr-preserves-behavior (`src/main.rs`): The `Telemetry` struct is constructed and wrapped in an Arc but is never used anywhere; every handler instead re-creates instruments via `global::meter(...)` on each request. With the router state broken, `telemetry` is dead code and the intended shared-instrument design is not wired in.
- qr-preserves-behavior (`src/main.rs`): Instruments (histograms, counters, up/down counters) are rebuilt via `global::meter().*.build()` on every request inside each handler, adding avoidable allocation/lookup overhead to the hot path rather than reusing the pre-built shared instruments.
- qr-preserves-behavior (`src/main.rs`): On the validation-failure path in todos_create the return type is `(StatusCode, Json<Todo>)` returning a synthetic empty Todo with a 422 status, whereas the success path returns `(StatusCode::CREATED, Json(todo))`; the original endpoint likely returned an error/empty body, so emitting a fake Todo body changes the response contract.
- qr-preserves-behavior (`src/main.rs`): OTLP metric/span exporters are built with `.expect(...)`, so a failure to construct the exporter (e.g. bad endpoint config) panics and aborts application startup, changing the original crash-free startup behavior.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): The proposed dependency features listed in proposedDependencies use "reqwest-blocking-client" but Cargo.toml enables "reqwest-client" for opentelemetry-otlp. With only the async "reqwest-client" feature and no runtime-driven exporter installation, MetricExporter::builder().with_http() may not resolve to a working HTTP client; more importantly the mismatch between the stated recipe and the enabled feature can leave the http exporter without a usable client. (Feature/client selection issue.)
- qr-preserves-behavior (`src/main.rs`): In todos_create the validation-failure path returns a 422 response built as `(StatusCode::UNPROCESSABLE_ENTITY, StatusCode::UNPROCESSABLE_ENTITY).into_response()`, but the success path returns `(StatusCode::CREATED, Json(todo))`; the two arms of this handler return different types and there is no `.into_response()` on the success arm, so the function will not compile (mismatched arm types / impl IntoResponse type unification).
- qr-preserves-behavior (`src/main.rs`): The 422 validation branch in todos_create uses `return ...`, but the success path is a tail expression `(StatusCode::CREATED, Json(todo))` without `.into_response()`, so the branches yield incompatible types and the early-return refactor changes/breaks the handler's return contract, failing compilation.
**Outstanding (2):**
- ⛔ qr-not-a-noop (`src/main.rs`:193): The SdkMeterProvider is built with `.with_periodic_exporter(exporter)` but the exporter is built via the `reqwest-blocking-client` feature; under the periodic reader running on the tokio runtime, a blocking reqwest client will panic/block the async runtime during export, and more critically the periodic reader requires an rt-tokio runtime configuration that is not set — with a blocking client the periodic export can fail to flush and emit no metrics. — _Use the async reqwest client feature (`reqwest-client`) instead of `reqwest-blocking-client` for the periodic metric exporter and batch span exporter, since they run on the tokio runtime._
- ⛔ qr-preserves-behavior (`src/main.rs`:230): The OTel provider shutdown code is placed after `axum::Server::serve().await.unwrap()`, which for a long-running server never returns normally; on the typical Ctrl-C/SIGTERM termination the process exits without ever calling `meter_provider.shutdown()`/`tracer_provider.shutdown()`, so buffered spans and the final metric batch are lost. — _Install a graceful shutdown signal (e.g. `with_graceful_shutdown`) so `serve().await` returns and the shutdown calls actually run before exit._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = { version = "0.28.0", features = ["metrics", "trace"] }`
- `opentelemetry_sdk = { version = "0.28.0", features = ["metrics", "trace", "rt-tokio"] }`
- `opentelemetry-otlp = { version = "0.28.0", features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
