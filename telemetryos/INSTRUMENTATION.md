# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (8):**
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = { version = "0.31", ", "opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration.
- qr-not-a-noop (`src/main.rs`): The PeriodicReader used by SdkMeterProvider::with_periodic_exporter requires the rt-tokio async runtime feature, but opentelemetry_sdk in the actual [dependencies] block only enables features ["metrics", "trace"] (the rt-tokio-enabling hunk is not reflected in the final Cargo.toml content). Without rt-tokio the periodic reader cannot spawn its export task and metrics will not be exported.
- general (`Cargo.toml`): opentelemetry-otlp is configured with default-features=false and features including "reqwest-blocking-client" (final content) while using the async PeriodicReader/BatchExporter; the http-proto exporter with a blocking reqwest client is mismatched with the tokio async runtime and the standard non-blocking reqwest-client feature expected for background export.
- qr-preserves-behavior (`src/main.rs`): The otel middleware wraps every request in a tracer span but TraceLayer::new_for_http() is also present, producing duplicated/overlapping request spans for the same HTTP request.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = { version = "0.31", "), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): opentelemetry-otlp enables both `reqwest-client` and `reqwest-blocking-client` features simultaneously in the final content, which is contradictory for the http-proto transport and can cause the wrong (blocking) client to be selected; the recipe requires the non-blocking reqwest client with the rt-tokio PeriodicReader.
- qr-not-a-noop (`src/main.rs`): The PeriodicReader used by SdkMeterProvider::with_periodic_exporter requires the async runtime feature (rt-tokio) on opentelemetry_sdk; the final Cargo.toml content lists opentelemetry_sdk features as ["metrics","trace"] without rt-tokio, so metric export would not run.
**Outstanding (3):**
- ⚠ qr-duplicate-metric (`src/main.rs`:243): Metric name "flow.outcomes" is created 2 times in this change (src/main.rs:243, src/main.rs:243). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ. — _Create the "flow.outcomes" instrument ONCE (a single shared meter/instrument reused across the call sites) and record to it from each place, rather than registering it in multiple files._
- ⛔ qr-not-a-noop (`src/main.rs`:40): The OTLP metric exporter is built with `default-features = false` and only the `reqwest-blocking-client` feature, but the PeriodicReader created by `with_periodic_exporter` requires the `rt-tokio` async runtime feature to spawn its background export loop. Without that feature the periodic reader has no runtime to drive it, so no metrics are ever exported. — _Add the `rt-tokio` feature to opentelemetry_sdk (and ensure the otlp transport client matches), so the PeriodicReader has an async runtime to flush metrics._
- ⚠ qr-not-a-noop (`Cargo.toml`:22): opentelemetry-otlp is configured with `reqwest-blocking-client` while the exporters are built and driven inside a tokio async runtime; the blocking reqwest client can panic or stall when invoked from within a tokio worker, preventing span/metric export. — _Use the non-blocking `reqwest-client` feature (with `rt-tokio`) instead of `reqwest-blocking-client` for use inside the tokio runtime._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = "0.31"`
