# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (6):**
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = { version = "0.31", ", "opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration.
- qr-not-a-noop (`Cargo.toml`): The PeriodicReader (via SdkMeterProvider::with_periodic_exporter) requires the async runtime feature (rt-tokio) on opentelemetry_sdk, but the final Cargo.toml enables only features ["metrics", "trace"] on opentelemetry_sdk. Without rt-tokio the periodic reader has no runtime and metrics will never be exported.
- general (`Cargo.toml`): opentelemetry-otlp is declared with both http-proto client features "reqwest-client" and "reqwest-blocking-client" simultaneously; the blocking client conflicts with the async PeriodicReader/batch exporter running under tokio and can cause a runtime panic (blocking reqwest inside async context) or feature conflict.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = { version = "0.31", ", "opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): The opentelemetry-otlp dependency enables both mutually-exclusive transport client features "reqwest-client" and "reqwest-blocking-client" at once. With a periodic reader on rt-tokio and an async runtime, the blocking client feature is inappropriate and the conflicting feature set can produce a broken/unexpected HTTP client selection.
- qr-preserves-behavior (`src/main.rs`): flow_freshness is recorded with a hardcoded 0.0 in todos_update, so the "wall clock time between flow entry and terminal state" histogram always emits zero and conveys no real signal; the flow_start captured in todos_create is never propagated to the update handler.
**Outstanding (2):**
- ⛔ qr-not-a-noop (`src/main.rs`:180): The OTLP exporters are built with `.with_http()` but the opentelemetry-otlp features enable `reqwest-blocking-client` (a blocking HTTP client) while the PeriodicReader for metrics and BatchSpanProcessor for traces require an async runtime exporter (rt-tokio / non-blocking reqwest client). With only the blocking client and no `rt-tokio` feature, the periodic reader/batch processor cannot drive the exporter, so metrics and spans will not be exported. — _Add the `rt-tokio` feature to opentelemetry_sdk and use a non-blocking reqwest client feature (e.g. `reqwest-client`) for opentelemetry-otlp so the PeriodicReader and BatchSpanProcessor can export asynchronously._
- ⛔ qr-not-a-noop (`src/main.rs`:195): `global::set_meter_provider` and `global::set_tracer_provider` return `()` (not a `Result`), so wrapping them in `if let Err(e) = std::panic::catch_unwind(...)` treats the unit return as the value; these functions also do not panic on re-registration (they silently overwrite), so the 'already registered' fallback is dead logic. Under app-sdk this is the sole registration and setting works, but the misleading catch_unwind reflects an incorrect conflict-handling assumption. — _Call `global::set_meter_provider(meter_provider.clone());` and `global::set_tracer_provider(tracer_provider.clone());` directly without catch_unwind/if-let._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
