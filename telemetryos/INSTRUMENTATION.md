# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (5):**
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): The [dependencies] table declares `tokio` twice (the original entry plus a duplicated one appended after opentelemetry-otlp) and also declares `opentelemetry-otlp` twice with conflicting feature sets (`reqwest-blocking-client` vs `reqwest-client`). Duplicate keys in the same table cause Cargo to fail to parse the manifest.
- general (`Cargo.toml`): The final opentelemetry-otlp entry enables both `reqwest-client` and `reqwest-blocking-client`. Combined with `with_http()` (which auto-selects a client), enabling the blocking reqwest client and building/using the exporter inside the tokio runtime can panic ("Cannot drop a runtime in a context where blocking is not allowed" / blocking client used inside async), contradicting the code comment stating async client is used.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): The opentelemetry-otlp dependency enables both "reqwest-client" (async) and "reqwest-blocking-client" features, but the SDK is initialized with .with_periodic_exporter/.with_batch_exporter inside a #[tokio::main] async runtime; the blocking reqwest client will panic when its synchronous HTTP call is executed on the batch/periodic worker thread within the Tokio runtime context. The recipe pairs http-proto with a single consistent client; mixing async and blocking clients here is misconfigured.
**Outstanding (2):**
- ⛔ qr-preserves-behavior (`src/main.rs`:140): The router calls both `.with_state(db)` and `.with_state(telemetry)`, but axum's `with_state` consumes the router and produces a `Router<()>` after the first call; you cannot chain two different state types this way. This fails to compile — extracting two distinct `State<Db>` and `State<TelemetryState>` requires a single combined state type implementing `FromRef` for each. — _Define a single AppState struct holding both `db` and `telemetry`, implement `FromRef<AppState>` for `Db` and `TelemetryState`, and call `.with_state(app_state)` once._
- ⛔ qr-preserves-behavior (`src/main.rs`:29): The `reqwest-blocking-client` feature configures a blocking HTTP exporter, and `.with_periodic_exporter`/`.with_batch_exporter` run their reader/processor threads that invoke this blocking client; using a blocking reqwest client inside the tokio async runtime can panic ("Cannot drop a runtime in a context where blocking is not allowed") or block the async executor. — _Use the async `reqwest-client` feature (non-blocking) with the tokio runtime, or run the exporter on a dedicated thread, consistent with the app-sdk recipe's tonic/http async idiom._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = { version = "0.28.0", features = ["metrics", "trace"] }`
- `opentelemetry_sdk = { version = "0.28.0", features = ["metrics", "trace"] }`
- `opentelemetry-otlp = { version = "0.28.0", default-features = false, features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
