# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (7):**
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): opentelemetry-otlp is declared with the "reqwest-client" feature, which pulls in the async reqwest client, but the metric provider uses `.with_periodic_exporter` with the tokio runtime and the http exporter—more critically, the proposedDependencies list specified "reqwest-blocking-client" while the diff uses "reqwest-client", and neither combination guarantees the http-proto transport is functional without a matching runtime feature. The mismatch between proposedDependencies and the actual Cargo.toml feature set risks a build/transport failure.
- qr-preserves-behavior (`src/main.rs`): SdkMeterProvider is built with `.with_periodic_exporter` while opentelemetry_sdk enables the "rt-tokio" feature, but the periodic reader requires a runtime to be specified; in opentelemetry_sdk 0.28 the periodic exporter spawns background tasks and, combined with the reqwest (async) exporter, may block or panic. More importantly, MetricExporter/SpanExporter `.with_http()` uses reqwest which needs the http-proto transport correctly wired; a runtime/exporter mismatch can cause telemetry export to fail silently.
- qr-preserves-behavior (`src/main.rs`): In todos_update the closure uses `db.read()` then later `db.write()` while holding no read guard across the write, but the read guard from `db.read().unwrap()` is dropped only at the end of the statement chain; the subsequent `db.write().unwrap()` within the same closure could deadlock on a non-reentrant RwLock if the read guard were still alive—however the guard is temporary so this is acceptable. The real behavior change is that todos_get/todos_update now clone under read lock adding latency, but that matches original; no action needed.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): The proposed dependency list specifies opentelemetry-otlp with the "reqwest-blocking-client" feature, but Cargo.toml enables "reqwest-client" instead. With rt-tokio periodic/batch exporters running on the async runtime, and no explicit HTTP client feature matching the intended blocking client, this mismatch can cause the OTLP HTTP exporter to lack a functioning client. More importantly, the inconsistency between proposedDependencies and the actual Cargo.toml means the wired feature set is not what the recipe intended.
- qr-preserves-behavior (`src/main.rs`): In todos_index, todos_get, todos_delete and the error branch, active_requests is incremented and later decremented, but if db.read().unwrap()/db.write().unwrap() panics on a poisoned lock, the function unwinds without decrementing active_requests, permanently leaking the in-flight gauge count. The original code already used unwrap(), but the added add(1)/add(-1) without RAII/guard makes the gauge inaccurate on any panic path.
**Outstanding (2):**
- ⛔ qr-not-a-noop (`src/main.rs`:45): SdkMeterProvider::builder().with_periodic_exporter() with the reqwest-blocking-client feature requires a runtime to drive periodic export, but no explicit rt-tokio wiring is used and the blocking reqwest client cannot be driven from within the tokio async runtime, so metric export will not function correctly under #[tokio::main]. The metric exporter should use an async client (reqwest-client / rt-tokio) rather than reqwest-blocking-client for a tokio app. — _Use the reqwest async client feature (e.g. "reqwest-client") instead of "reqwest-blocking-client" so the periodic metric reader can export within the tokio runtime._
- ⚠ qr-preserves-behavior (`src/main.rs`:95): meter_provider.shutdown() and tracer_provider.shutdown() are placed after axum::Server::...await, which only returns on server termination; combined with the blocking exporter under tokio, buffered telemetry flush on graceful shutdown is unreliable, but this does not alter the original request-handling behavior. — _Install a graceful shutdown signal handler and flush providers on shutdown, ensuring the async exporter is used._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = { version = "0.28.0", features = ["metrics", "trace"] }`
- `opentelemetry_sdk = { version = "0.28.0", features = ["metrics", "trace", "rt-tokio"] }`
- `opentelemetry-otlp = { version = "0.28.0", features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
