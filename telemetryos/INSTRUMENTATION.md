# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (4):**
- qr-not-a-noop (`src/main.rs`): An observable gauge (`_active_requests_observable`) is created inside the per-request middleware on every request via `global::meter(...)` with a registered callback. Each invocation registers a NEW async instrument+callback on the meter, so after N requests the SDK invokes N duplicate callbacks all reporting the same ACTIVE_REQUESTS value, producing conflicting/duplicated time series (and unbounded instrument accumulation).
- general (`Cargo.toml`): The opentelemetry-otlp dependency lists features "metrics" and "trace" that are not valid features on that crate (the OTLP crate uses signal features but its actual feature names differ; "metrics"/"trace" are not exported), which will fail cargo feature resolution.
- general (`src/main.rs`): The constant WORKER_POOL_SIZE is declared but never used, producing a compiler warning (and denial if warnings are treated as errors).
- qr-not-a-noop (`src/main.rs`): The static ACTIVE_REQUESTS counter is incremented/decremented on every request but its value is never read or reported to any metric instrument, so it emits no telemetry and is dead accounting.
**Outstanding (2):**
- ⚠ qr-not-a-noop (`src/main.rs`:110): The per-request metric instruments (duration_histogram, active_requests_gauge, outcome_counter, flow_entry_counter) are re-created on every request via meter.build() inside the middleware. Recreating instruments per-call is wasteful and can produce duplicate-instrument warnings; instruments should be created once and reused. — _Create the instruments once at startup (e.g. store them in state or a OnceCell/lazy static) and reference them in the middleware, rather than rebuilding them on each request._
- ⛔ edit-not-applied (`Cargo.toml`): An intended edit to Cargo.toml could not be applied (its target text wasn't found verbatim, wasn't unique, or overlapped another edit), so the file was left unchanged to avoid a half-applied result: rejected: non-additive manifest edit (would drop "opentelemetry-otlp = { version = "0.31", default-features = ") — re-emit with newString = the entire oldString plus the additions. Remove invalid metrics/trace features from opentelemetry-otlp dependency — _Re-express this as one or more SMALL hunks whose oldString is copied verbatim from the CURRENT source and is unique; don't restate a large block. Keep declarations and their uses in edits that apply together._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client"] }`

## Edits that need manual application
These instrumentation changes couldn't be applied automatically (the target code had moved or
wasn't unique). Apply them by hand:

### `Cargo.toml` — rejected: non-additive manifest edit (would drop "opentelemetry-otlp = { version = "0.31", default-features = ") — re-emit with newString = the entire oldString plus the additions. Remove invalid metrics/trace features from opentelemetry-otlp dependency
```
opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client"] }
```

