# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⚠ passed with warnings.
**Auto-fixed during review (2):**
- qr-preserves-behavior (`src/main.rs`): The active_requests UpDownCounter is incremented at the top of todos_update and todos_get, but on the RwLock poison path (`.unwrap()` panicking on a poisoned lock) the decrement never runs; more importantly, no explicit early-return leaks the counter in normal flow. However the confirmed leak is real: any handler that panics between the `add(1,...)` and the corresponding `add(-1,...)` (e.g. an unwrap on a poisoned lock) permanently inflates active_requests since there is no RAII guard.
- qr-preserves-behavior (`src/main.rs`): In todos_update, the read lock guard from state.db.read().unwrap() inside the ok_or_else chain is held while the closure executes, and later state.db.write().unwrap() is called after the guard from the read().unwrap()...cloned() expression is dropped — but the RwLock read guard from the initial read is dropped after .cloned(), so this is fine. However the .add(-1) decrement on active_requests is duplicated: the 404 error closure decrements active_requests and the success path also decrements, which is correct, but on the 404 path the flow still returns via ?, so no double count. This is acceptable.
**Outstanding (1):**
- ⚠ qr-preserves-behavior (`src/main.rs`): Metric instruments are created via global::meter with dotted OTLP-style names but the periodic exporter and reqwest-blocking-client feature run a blocking HTTP client inside a tokio async runtime; with_periodic_exporter/with_batch_exporter using a blocking reqwest client can block the async reactor or panic at export time. Prefer the async reqwest-client feature over reqwest-blocking-client under a tokio runtime. — _Use `reqwest-client` (async) feature instead of `reqwest-blocking-client` for opentelemetry-otlp when running under tokio._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = { version = "0.28.0", features = ["metrics", "trace"] }`
- `opentelemetry_sdk = { version = "0.28.0", features = ["metrics", "trace"] }`
- `opentelemetry-otlp = { version = "0.28.0", default-features = false, features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
