# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (5):**
- qr-not-a-noop (`src/main.rs`): The MatchedPath extension is not available in the telemetry_middleware because it runs after TraceLayer but MatchedPath is populated by axum's routing; more critically, the middleware ordering places axum::Extension(metrics) AFTER middleware::from_fn(telemetry_middleware) in ServiceBuilder, so with tower/ServiceBuilder layering order the Extension layer wraps outermost and runs first — but the http.route reported will fall back to "unmatched" for many requests. This is a warning-level correctness concern rather than a hard no-op.
- qr-not-a-noop (`src/main.rs`): The metrics ServiceBuilder layer ordering was reordered so axum::Extension(metrics) is applied AFTER middleware::from_fn(telemetry_middleware); in tower ServiceBuilder, layers execute top-to-bottom on request, so telemetry_middleware runs before the Extension is inserted into request extensions, causing req.extensions().get::<Arc<Metrics>>() to return None and all request/duration/outcome metrics to be silently skipped.
- qr-preserves-behavior (`src/main.rs`): In todos_update the flow_freshness histogram is always recorded with a hardcoded 0.0 value, and completed_now is set from input.completed.unwrap_or(false) meaning the success outcome fires on every PATCH that sets completed=true regardless of prior state, producing misleading telemetry.
- qr-not-a-noop (`src/main.rs`): The telemetry_middleware and handlers extract the Metrics from a request extension (`req.extensions().get::<Arc<Metrics>>()`), but the middleware is added to the ServiceBuilder BEFORE `.layer(axum::Extension(metrics))`. In a tower ServiceBuilder the layers wrap outermost-first, so `axum::Extension(metrics)` runs after `telemetry_middleware` in the request path — the Extension is inserted inside/after the middleware runs, meaning the extension may not be present. Worse, `telemetry_middleware`'s `.expect("Metrics extension missing")` will panic on every request if the extension is not visible at that layer, aborting the request.
- qr-preserves-behavior (`src/main.rs`): telemetry_middleware uses `.expect("Metrics extension missing")` which panics if the Arc<Metrics> extension is absent, turning a previously working request into a panic/aborted connection — an instrumentation-introduced behavior change on the hot path.
**Outstanding (3):**
- ⛔ qr-not-a-noop (`src/main.rs`): The telemetry_middleware reads MatchedPath from req.extensions(), but in axum/tower ServiceBuilder the layers execute in the order listed (outermost first). MatchedPath is inserted by axum's routing which runs after the middleware stack; combined with the observability metrics recording per-route, but the bigger concern is route attribute: MatchedPath may not be populated for the from_fn middleware placed at the ServiceBuilder level, so http.route will always resolve to "unmatched", producing incorrect/low-cardinality telemetry. — _Register the telemetry middleware as a route_layer or per-router layer so MatchedPath is available, or extract the route after routing resolves._
- ⚠ qr-not-a-noop (`src/main.rs`): The Metrics struct declares an `auth_attempts` counter that is created in build_metrics() but never incremented anywhere, so this instrument emits nothing at runtime. — _Remove the unused auth_attempts counter or add code that records auth decisions._
- ⚠ qr-not-a-noop (`src/main.rs`): The flow_freshness histogram is always recorded with a hardcoded 0.0 value, so the metric conveys no meaningful wall-clock time between flow entry and terminal state. — _Track the actual create timestamp (e.g. store it on the Todo) and record the real elapsed duration, or remove the metric._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client"] }`
