# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (8):**
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): The proposed dependencies list specifies opentelemetry-otlp with the "reqwest-blocking-client" feature, but Cargo.toml enables "reqwest-client" instead. The reqwest (non-blocking) client requires a running Tokio runtime and a compatible metric reader; used with .with_http() and .with_periodic_exporter it can fail to export/build correctly compared to the recommended blocking client.
- qr-preserves-behavior (`src/main.rs`): In todos_update, the todo is read with an early-returning closure (ok_or_else + ?) that decrements active_requests and records duration; but on the success path the todo is read via state.db.read() while a later state.db.write() re-inserts it — the read lock guard is dropped correctly, so no deadlock, but the error closure runs while the read lock is still held (inside get(&id).cloned().ok_or_else), and it calls state.telemetry which is fine; however this does not alter control flow. Omit.
- qr-preserves-behavior (`src/main.rs`): active_requests is incremented at handler entry but is not decremented on panic paths (e.g., an unwrap() on a poisoned RwLock), so the UpDownCounter can drift upward and permanently overcount in-flight requests after any panic.
- qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry-otlp = { version = "0.28.0"), so it removes or rewrites existing build configuration.
- general (`Cargo.toml`): The proposed dependency snippet lists opentelemetry-otlp with the "reqwest-blocking-client" feature, but the actual Cargo.toml enables "reqwest-client" instead. The `.with_http()` builder without an explicit blocking client requires the blocking reqwest client feature; with only the async "reqwest-client" feature, building the HTTP exporter in a synchronous context (SdkMeterProvider/SpanExporter setup) will fail to build or panic at runtime.
- general (`Cargo.toml`): serde_json is used in src/main.rs (serde_json::json! in the validation error path of todos_create) but is not declared as a dependency in Cargo.toml, so the crate will fail to compile.
- qr-preserves-behavior (`src/main.rs`): The active_requests UpDownCounter is incremented at the start of each handler but is not decremented on early-return panics from `.unwrap()` on the RwLock or on JSON extraction rejections; while explicit error paths decrement it, a poisoned-lock panic leaves active_requests permanently inflated.
**Outstanding (3):**
- ⛔ qr-preserves-behavior (`src/main.rs`:250): In todos_create the validation-failure branch returns `(StatusCode::UNPROCESSABLE_ENTITY, Json(Todo{...}))` while the success branch returns `(StatusCode::CREATED, Json(todo))`, but the original handler's return type differs; more critically both arms return the same tuple type so this compiles, yet the error arm now returns a fabricated empty Todo body instead of the original error response, changing the API's response shape and behavior.
- ⛔ qr-preserves-behavior (`src/main.rs`:140): The metrics pipeline uses `MetricExporter::builder().with_http()` with a periodic reader inside a `#[tokio::main]` async runtime, but the enabled feature is `reqwest-blocking-client`; the blocking reqwest client cannot run inside a Tokio async context and will panic at runtime ("Cannot drop a runtime in a context where blocking is not allowed" / blocking client used in async), preventing metric export.
- ⚠ qr-preserves-behavior (`src/main.rs`:205): active_requests UpDownCounter is incremented at handler entry but in todos_update and todos_get the decrement only occurs in the `ok_or_else` closure and the success path; however the early return via `?` from `ok_or_else` does decrement, but any panic on `.unwrap()` of a poisoned RwLock leaves active_requests permanently incremented, and there is no drop-guard, risking a leaked in-flight count.

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = { version = "0.28.0", features = ["trace"] }`
- `opentelemetry_sdk = { version = "0.28.0", features = ["trace", "metrics"] }`
- `opentelemetry-otlp = { version = "0.28.0", features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
