# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (11):**
- qr-duplicate-metric (`src/main.rs`): Metric name "http.server.request.duration" is created 2 times in this change (src/main.rs:46, src/main.rs:46). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-duplicate-metric (`src/main.rs`): Metric name "http.server.requests.outcome" is created 2 times in this change (src/main.rs:52, src/main.rs:52). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-duplicate-metric (`src/main.rs`): Metric name "flow.outcomes" is created 3 times in this change (src/main.rs:57, src/main.rs:57, src/main.rs:57). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-duplicate-metric (`src/main.rs`): Metric name "flow.duration" is created 2 times in this change (src/main.rs:62, src/main.rs:62). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-duplicate-metric (`src/main.rs`): Metric name "flow.entries" is created 2 times in this change (src/main.rs:68, src/main.rs:68). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-duplicate-metric (`src/main.rs`): Metric name "flow.validation.outcomes" is created 2 times in this change (src/main.rs:73, src/main.rs:73). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-not-a-noop (`src/main.rs`): The meter_provider and tracer_provider are created and set globally inside main(), but they are never bound to a variable that outlives the block, and provider.shutdown() is never called on exit. Without holding the providers and shutting them down, buffered metrics/spans are dropped at process exit and the periodic/batch exporters may never flush, losing telemetry.
- general (`src/main.rs`): The MatchedPath extension is read inside a middleware attached via `.route_layer(...)`, but route_layer runs on matched routes so http.route should resolve; however the fallback (404) path is not covered by route_layer, so unmatched requests emit no telemetry and the 'unmatched' branch is effectively dead.
- general (`src/main.rs`): The Metrics struct instruments created in init_metrics() (http_server_duration, flow counters, etc.) are stored as an Extension but never used; the middleware and handlers instead re-create instruments ad hoc via global::meter each call, so the entire Metrics struct is dead weight.
- qr-preserves-behavior (`src/main.rs`): todos_create and todos_update now take axum::Extension<Arc<Metrics>> as an extractor, but the Extension layer is applied via `.layer(axum::Extension(Arc::new(metrics)))` AFTER `.with_state(db)` and the routes; the Extension must be present for the handler to run, and since telemetry_middleware also requires this Extension while being applied at the top level, the ordering interacts but the Extension is registered so this resolves — the real problem is the handler signatures require the Extension extractor which changes the handler's argument order/type and any test or caller invoking them directly will break.
- qr-not-a-noop (`src/main.rs`): The `auth_attempts` counter is created in init_metrics and stored in Metrics but is never incremented anywhere, so it emits no data.
**Outstanding (1):**
- ⛔ qr-preserves-behavior (`src/main.rs`:204): The telemetry_middleware is attached via `.layer(...)` AFTER `.layer(axum::Extension(Arc::new(metrics)))` in the outer builder. Because axum/tower layers apply in reverse (outermost first), the middleware added last runs outermost — before the Extension layer inserts the Arc<Metrics> into request extensions — so `axum::Extension(metrics)` extraction in the middleware will fail and every request will error, breaking all routes. — _Attach the telemetry_middleware layer before (below) the Extension layer, or extract metrics via a captured Arc using from_fn_with_state instead of relying on request-extension ordering._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client"] }`
