# Instrumentation for Axum-todo

Generated by TelemetryOS against `7e29277`. This PR edits the existing source to
implement the agreed SLIs.

## Setup & self-review

**Detected setup:** rust · cargo · host: unknown · telemetry: none → wiring model **app-sdk** _(low confidence — please confirm)_
**Self-review:** 3 generation passes — ⛔ unresolved blocking issues — opened as DRAFT.
**Auto-fixed during review (10):**
- general (`src/main.rs`): The router calls both `.with_state(db)` and `.with_state(http_metrics)`, giving the Router a single combined state type. But handlers use `State<Db>`, `State<Arc<HttpMetrics>>`, and the middleware uses `State<Arc<HttpMetrics>>`; axum's `with_state` fully applies one state type and returns `Router<()>`, so the second `with_state` and the mixed `State<T>` extractors cannot both be satisfied. This is a runtime/wiring defect where the metrics state cannot be provided to handlers as written.
- qr-preserves-behavior (`src/main.rs`): The metric exporter is built with `MetricExporter::builder().with_http().build().expect(...)` and the span exporter likewise uses `.expect(...)`; if the OTLP endpoint/config is missing at startup these panic, changing existing behavior by crashing the process on boot where the original binary started successfully.
- qr-preserves-behavior (`src/main.rs`): The middleware attaches a span context and holds `_guard` across `next.run(req).await`, but the span created by `tracer.start(...)` is stored inside the Context and never explicitly ended; while drop ends it, `cx.span()` is used after `next.run` and the manual span duplicates the coverage of the already-present TraceLayer, producing overlapping/duplicate server spans per request.
- general (`Cargo.toml`): opentelemetry-otlp is configured with both `reqwest-client` and `reqwest-blocking-client` features while using a periodic reader that requires an async runtime; the recipe/target notes state PeriodicReader needs `rt-tokio` on opentelemetry_sdk, but the final Cargo.toml sets `opentelemetry_sdk = { version = "0.31", features = [] }` without `rt-tokio`, so metric export may not run on the tokio runtime.
- qr-duplicate-file-path (`src/main.rs`): "src/main.rs" appears 5× in the changeset (edited file + edited file + edited file + edited file + edited file) — two conflicting full contents for one path. Only the last-written wins, and it may drop declarations/imports the other version added, so the file won't compile.
- qr-duplicate-metric (`src/main.rs`): Metric name "flow.outcomes" is created 2 times in this change (src/main.rs:126, src/main.rs:161). Duplicate instrument registrations under one name produce merged/duplicate streams (double-counting), or an export conflict if their kind/unit differ.
- qr-not-a-noop (`src/main.rs`): The PeriodicReader created by SdkMeterProvider::with_periodic_exporter requires the async runtime feature (rt-tokio), but opentelemetry_sdk is declared without it. Without a runtime, the periodic reader cannot export and metrics will never be flushed to the collector.
- general (`Cargo.toml`): opentelemetry-otlp is configured with `reqwest-blocking-client` for the http-proto transport, but a blocking reqwest client used inside the Tokio async runtime (PeriodicReader/BatchProcessor on rt-tokio) will attempt blocking I/O on an async context. The recommended client is the non-blocking `reqwest-client`.
- qr-not-a-noop (`src/main.rs`): The i64_observable_up_down_counter and i64_observable_gauge instruments are created with no callback registered (the closure block only clones the Arc and drops it), so http.server.active_requests and http.server.worker_pool.size never report any value and emit nothing.
- general (`src/main.rs`): The middleware handler uses the axum 0.7 `Next` signature (no generic body parameter) and `middleware::from_fn_with_state`, while the crate pins axum = "0.6.12"; in axum 0.6 `Next` is generic (`Next<B>`) and the run signature differs, so this will not compile against the declared version.
**Outstanding (5):**
- ⛔ qr-build-additive (`Cargo.toml`): Non-additive edit to Cargo.toml: the replacement drops existing line(s) ("opentelemetry_sdk = { version = "0.31" }", "opentelemetry-otlp = { version = "0.31","), so it removes or rewrites existing build configuration. — _Make build-file edits additive: oldString = a small existing anchor, newString = that anchor plus the new dependency line(s). Never drop or rewrite an existing line._
- ⛔ qr-not-a-noop (`src/main.rs`:49): The SdkMeterProvider is built with a PeriodicReader (with_periodic_exporter) which requires an async runtime, but the Cargo.toml final content has opentelemetry_sdk without the rt-tokio feature (features = []). Without rt-tokio the periodic reader has no runtime to drive it, so metrics are never exported at runtime. — _Enable the rt-tokio feature on opentelemetry_sdk in the final Cargo.toml so the PeriodicReader can flush metrics._
- ⚠ qr-not-a-noop (`src/main.rs`:388): todos_create and todos_update use State(metrics): State<Arc<HttpMetrics>> extractors, but the Router is composed with .with_state(db) providing only Db state. Axum cannot supply Arc<HttpMetrics> as handler state, so these handlers do not satisfy the Handler trait and the router won't accept them. — _Use a combined application state (e.g. a struct holding both Db and Arc<HttpMetrics> with FromRef impls) rather than expecting two independent State extractors from a single-typed router state._
- ⚠ general (`Cargo.toml`:19): opentelemetry-otlp is configured with both reqwest-client and reqwest-blocking-client features while using the async tokio runtime; mixing the blocking reqwest client with the async periodic/batch exporters is contradictory and reqwest-blocking-client is unnecessary for this async setup. — _Use only reqwest-client (async) for the http-proto transport under the tokio runtime and drop reqwest-blocking-client._
- ⚠ qr-preserves-behavior (`src/main.rs`:150): The middleware attaches a new root span via Context::current_with_span for every request but never explicitly ends it; while the guard drop ends the context attachment, the span created by tracer.start is a new root rather than a child of the TraceLayer span, producing duplicated/disconnected trace hierarchy alongside the existing tower-http TraceLayer instrumentation. — _Reuse or parent to the existing TraceLayer span, or remove one of the two overlapping HTTP span sources to avoid duplicate root spans per request._

## SLIs implemented
- **Axum-todo — HTTP Service Availability** (`axum-todo-http-availability`) — target 99.95% over 30-day
- **Axum-todo — HTTP Response Time P95** (`axum-todo-http-latency-p95`) — target < 200ms over 1-hour
- **Axum-todo — HTTP Response Time P99** (`axum-todo-http-latency-p99`) — target < 750ms over 1-hour
- **Axum-todo — HTTP 5xx Error Rate** (`axum-todo-http-error-rate`) — target < 0.1% over 1-hour
- **Axum-todo — HTTP Worker Pool Saturation** (`axum-todo-http-saturation`) — target < 80% over 5-minute
- **Axum-todo — Authentication Failure Rate** (`axum-todo-http-auth-failure-rate`) — target < 2% over 1-hour
- **Axum-todo — HTTP Request Throughput** (`axum-todo-http-request-rate`) — target baseline ± 30% over 5-minute
- **Primary Flow — E2E Business Flow Success Rate** (`flow-primary-flow-e2e-flow-success`) — target 99.95% over 30-day
- **Primary Flow — E2E Business Flow Latency P95** (`flow-primary-flow-e2e-flow-latency`) — target < 500ms over 24-hour
- **Primary Flow — Flow Validation Failure Rate** (`flow-primary-flow-e2e-validation-failure-rate`) — target < 5% over 24-hour
- **Primary Flow — E2E Business Flow Throughput** (`flow-primary-flow-e2e-flow-throughput`) — target baseline ± 25% over 1-hour
- **Primary Flow — Flow Completion Freshness** (`flow-primary-flow-e2e-flow-freshness`) — target < 2 minutes over 1-hour

## Modified files
- `Cargo.toml`
- `src/main.rs`

## Dependencies
- `opentelemetry = "0.31"`
- `opentelemetry_sdk = "0.31"`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-blocking-client", "metrics", "trace"] }`
- `opentelemetry_sdk = { version = "0.31", features = ["rt-tokio"] }`
- `opentelemetry-otlp = { version = "0.31", default-features = false, features = ["http-proto", "reqwest-client", "metrics", "trace"] }`
